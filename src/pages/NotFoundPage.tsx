import Alert from '@mui/material/Alert';

const NotFoundPage = () => {
  return (
    <Alert variant="filled" severity="warning">
      Ooops, this page can't be found
    </Alert>
  );
};

export default NotFoundPage;
